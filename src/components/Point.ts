class Point {
    x: number
    y: number
    color: string
    size: string
    constructor(x: number, y: number, color: string, size: string) {
        this.x = x;
        this.y = y;
        this.color = color;
        this.size = size;
    }
};

export default Point;
